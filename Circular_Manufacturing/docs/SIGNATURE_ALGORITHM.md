# CIRCULAR-MASS Signature Algorithm

The canonical executable import is `src/circular4x/signature_algorithm.py`, backed by the project-native implementation in `src/circular_battery/optimization/signature_algorithm.py`.

See `docs/CIRCULAR_MASS.md` and `docs/RESEARCH_VALIDATION_CIRCULAR_MASS.md` for formulation, baselines, evidence classes, and claim boundaries.
