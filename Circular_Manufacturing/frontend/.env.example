# Base URL of the circular_battery Python backend (server.py / run_workbench.py).
# Local default matches `python scripts/run_workbench.py` (http://127.0.0.1:8765).
# In Vercel's project settings, set this to the deployed Render backend URL,
# e.g. https://circular-material-studio.onrender.com
VITE_API_BASE=http://127.0.0.1:8765
